---
import Navbar from "../components/Navbar.astro";
import { Icon } from "astro-icon";

import Footer from "../components/Footer.astro";
import Modal from "../components/Modal.astro";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta
      name="description"
      content="Estudio de tatuajes de la ciudad de Vedia, Tatuajes ASrtisticos, Cover Up, restauracion de tatuajes antiguos, Variedad de Estilos"
    />
    <meta name="description" content="galeria de Imagenes de Pichon Tattoo" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <div id="btn">
      <Icon name="mdi:menu" id="menu" />
      <Icon name="mdi:close" id="close" />
    </div>
    <Navbar />

    <slot />

    <Footer />
  </body>
</html>
<style is:global>
  [astro-icon] {
    display: none;
    width: 30px;
    margin-left: 90%;
    margin-top: 3%;
    position: fixed;
    color: white;
    z-index: 999;
    cursor: pointer;
  }
  :root {
    --accent: 124, 58, 237;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      #da62c4 30%,
      white 60%
    );
  }
  * {
    margin: 0%;
    padding: 0%;
    box-sizing: border-box;
  }
  html {
    scroll-behavior: smooth;
  }

  body {
    width: 100%;
    height: 100vh;
    background-image: url("/Img/body-back.webp");
    background-attachment: fixed;
    background-size: cover;
    z-index: -50;
  }
  #btn {
    z-index: 999;
  }

  @media (max-width: 768px) {
    [astro-icon] {
      display: block;
    }
    #close {
      display: none;
    }

    .closeBtn {
      display: block;
    }
  }
</style>
<script></script>
